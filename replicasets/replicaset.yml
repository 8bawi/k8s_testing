apiVersion: apps/v1
kind: ReplicaSet
metdata:
  name: myapp-relicaset
  lables:
    app: myapp

spec:                
  selector:
    matchLabels:
      app: myapp
  replicas: 3
  template:
    metadata:
      name: nginx-2
      labels: 
        app: myapp
    spec:
      containers:
        - name: nginx
          image: nginx             

